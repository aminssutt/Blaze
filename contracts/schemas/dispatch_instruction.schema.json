{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://blaze.local/contracts/schemas/dispatch_instruction.schema.json",
  "title": "DispatchInstruction",
  "description": "One approved, unit-specific dispatch message generated by the Dispatch Agent after human approval.",
  "type": "object",
  "properties": {
    "dispatch_id": {
      "type": "string",
      "description": "Unique identifier of this dispatch instruction."
    },
    "plan_id": {
      "type": "string",
      "description": "Identifier of the approved plan version."
    },
    "unit_id": {
      "type": "string",
      "description": "Recipient unit (e.g. alpha-3)."
    },
    "priority": {
      "type": "string",
      "enum": ["low", "medium", "high", "critical"],
      "description": "Message priority."
    },
    "message_text": {
      "type": "string",
      "description": "Exact TTS-ready French message text."
    },
    "acknowledgement_required": {
      "type": "boolean",
      "description": "True if the unit must acknowledge reception."
    },
    "tts_audio_path": {
      "type": ["string", "null"],
      "description": "Path to the generated TTS WAV, null until generated."
    },
    "generated_at": {
      "type": "string",
      "format": "date-time",
      "description": "Generation time (ISO 8601)."
    },
    "dispatch_status": {
      "type": "string",
      "enum": ["pending", "tts_generating", "ready", "sent", "acknowledged", "failed"],
      "description": "Current status in the simulated dispatch pipeline."
    }
  },
  "required": [
    "dispatch_id",
    "plan_id",
    "unit_id",
    "priority",
    "message_text",
    "acknowledgement_required",
    "generated_at",
    "dispatch_status"
  ]
}
