# BLAZE STT — faster-whisper (local, offline after first model download)
# Install (from speech/stt/; ".venv" is covered by the repo .gitignore):
#   python -m venv .venv
#   .venv\Scripts\pip install -r requirements.txt
faster-whisper>=1.1.0
# Pinned: av 18.x (pulled by default) is blocked by Windows Smart App Control
# (unsigned brand-new wheel, no cloud reputation yet). 13.1.0 loads fine.
av==13.1.0
# Tests (issue #12): real-audio pytest suite + contract validation
pytest>=8.0
jsonschema>=4.21
